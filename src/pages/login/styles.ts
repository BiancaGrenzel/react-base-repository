const useStyles = () => ({
  container: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    height: "auto",
    background: "white",
    padding: "32px",
    borderRadius: "8px",
  },
});

export default useStyles;
